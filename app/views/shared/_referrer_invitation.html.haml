.center_text
  - if referrer.role == 'influencer'
    = image_tag(referrer.influencer.twitter_image_url, style: 'display: inline')
    = "Fuiste invitado al sitio por #{referrer.influencer.full_name}"
  - else
    = "Fuiste invitado al sitio por #{referrer.full_name}"
