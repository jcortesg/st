.row.registration_container
  .span6.offset3
    = simple_form_for(@user, :url => change_password_path(@user), :html => { class: "form-horizontal round_form", method: :put }) do |f|
      .form_title= t('change_password')
      = render '/shared/simple_form_errors', :f => f
      = f.input :password
      = f.input :password_confirmation

      .form-actions
        = f.submit t('save'), :class => 'btn btn-primary btn-large'
        = link_to t('cancel'), influencer_profile_path, class: 'btn btn-large'
