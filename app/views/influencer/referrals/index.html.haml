- if flash.any?
  .row
    .span12
      = render '/shared/flash', flash: flash

.row
  .span8
    .round_box
      .content
        .title Referidos

        %p
          = "Referiste #{@referrals.count} personas que te genero $0 ingresos"

        %p
          ¿No estás conforme? ¡Probá refiriendo más amigos!

        .title Como funciona

        %p
          Utilizá tu link para sumar gente a la plataforma de Borwin y comenzá a generar ingresos.
          Cuando una persona se registre utilizando tu link recibirás el 5% de los ingresos que la
          persona referida genere en Borwin a través de sus campañas.

        .title Tu link

        = "http://borwin.net/I#{current_user.invitation_code}"

        %p
          Este es el link que le tenés que envíar a las personas que referís y a través del cuál ellos se tienen
          que dar de alta en Borwin.


  .span4
    .round_box
      .content
        %ul.nav.nav-pills.nav-stacked
          %li.active
            = link_to("Información", [:influencer, :referrals])
          %li
            = link_to("Listado", [:list, :influencer, :referrals])

        %hr

        = simple_form_for @user, url: [:update_options, :influencer, :referrals], html: { class: "form-horizontal", method: :put } do |f|
          = f.input :mail_on_referral_singup, label: "Enviarme un email cuando un usuario se registre con mi código de referido"
          = f.input :mail_on_referral_profit, label: "Enviarme un email cuando mis referidos me generen ingresos"

          .form-actions
            = f.submit 'Guardar', :class => 'btn btn-primary btn-large'




