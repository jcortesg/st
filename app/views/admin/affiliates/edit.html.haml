.row
  .span6.offset3.round_box
    = simple_form_for(@user, url: [:admin, @affiliate], html:  { class: "form-horizontal round_form", method: :put }) do |f|
      %h1.form_title Agencia
      = render partial: 'form', locals: {f: f}

      .form-actions
        = f.submit 'Guardar', :class => 'btn btn-primary btn-large'
        = link_to 'Cancelar', [:admin, :affiliates], class: 'btn btn-large'
